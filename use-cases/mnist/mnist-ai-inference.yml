# Inference configuration
inference:
  type: lightning
  # Run ID in MLFlow server: pre-trained model
  # IMPORTANT: this has to be updated!
  run_id: 05ddff17a2d9422892c520beebcd4a18
  ckpt_path: model/checkpoints/best-checkpoint-v2/best-checkpoint-v2.ckpt
  train_config_artifact_path: pl-training.yml
  conf:
    data: null