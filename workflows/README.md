# Workflow manager integration

It is possible that `itwinai` is a step in a greater workflow.
This folder contains examples on how to execute an `itwinai` use case
from an external workflow manager, using its own workflow definition language.
